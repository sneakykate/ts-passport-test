{
  "name": "ts-passport-test",
  "version": "1.0.0",
  "description": "set up ts and auth",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kate",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1",
    "passport": "^0.3.2"
  }
}
